# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Environments
.venv/
.venv-*/
venv/
env/
ENV/
.env

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# The compiled catalog IS committed: it is small, and without it a clone-and-
# install has no data at all. CI proves it matches catalog/ on every push, so it
# cannot silently drift. Rebuild with `loadout catalog build`.
*.db.building

# Local test scratch
.testhome/
.t/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
