id: dscan
summary: wrapper around nmap
description: This package provides a wrapper around nmap, and distribute scans across several hosts. It
  aggregates / splits address ranges, uses a configuration file where scan configuration can be adjusted,
  supports resume.
categories:
- other
binaries:
- dscan
homepage: https://github.com/0x4E0x650x6F/dscan
size: 97280
install:
- provider: apt
  package: dscan
  distros:
  - kali
  - debian
  - parrot
  - ubuntu
version: 0.1.5-0kali1
