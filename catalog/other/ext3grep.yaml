id: ext3grep
summary: tool to help recover deleted files on ext3 filesystems
description: ext3grep is a simple tool intended to aid anyone who accidentally deletes a file on an ext3
  filesystem, only to find that they wanted it shortly thereafter. This package is useful in forensics
  investigations.
categories:
- forensics
size: 306176
binaries:
- ext3grep
install:
- provider: apt
  package: ext3grep
  distros:
  - kali
  - debian
  - parrot
  - ubuntu
version: 0.10.2-6
