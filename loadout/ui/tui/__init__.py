"""Textual TUI subpackage (optional).

Activated by running ``loadout`` with no arguments; requires the
optional ``textual`` dependency. Falls back to the Rich interface when
Textual is unavailable or when stdout is not a TTY.
"""
