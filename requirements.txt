# Dependencies are declared in pyproject.toml (PEP 621).
# Install from the project root with:
#     pip install -r requirements.txt
#
# This pulls in all runtime + optional dependencies so nothing is missing.

# --- Core ---
rich>=13.7
requests>=2.31
beautifulsoup4>=4.12

# --- Optional: desktop notifications ---
notify2>=0.3.1

# --- Optional: disk-space pre-check ---
psutil>=5.9

# --- Optional: full-screen Textual TUI ---
textual>=0.60

# --- Optional: fuzzy search ---
rapidfuzz>=3.5

# --- Dev / test ---
pytest>=7.4
pytest-mock>=3.12
ruff>=0.4
